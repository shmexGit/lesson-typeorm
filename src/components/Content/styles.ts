import { BoxProps } from "ink";

const styles = {
  container: {
    width: 90,
    flexDirection: 'column',
  } as BoxProps,
  title: {
    paddingLeft: 1,
    borderStyle: 'single',
  } as BoxProps,
  titleText: {
    bold: true,
    backgroundColor: 'blue',
  },
};

export default styles;